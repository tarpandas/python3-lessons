import math
x = 3.543
print(round(x)) # It will round the number
print(abs(-34.533)) # abs() is absolute value. It will always return positive

print(math.ceil(x)) # Rounds to the lower integer number
print(math.floor(x)) # Rounds to the higher integer number
print(math.sqrt(16)) # Square root